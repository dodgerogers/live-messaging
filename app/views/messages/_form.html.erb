<%= form_for Message.new, remote: true do |f| %>
	<div class="input-append">
		<%= f.label :text, raw("<i class='icon-comment'></i> Message") %>
		<%= f.text_field :text %>
		<%= f.submit "Send", class: "btn btn-success"%>
	</div>
<% end %>	